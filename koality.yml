languages:
  nodejs: 0.8.12
setup:
- packages:
  - npm:
    - npm install
test:
  scripts:
    - launch server test:
        - |
          iced server.coffee &
          serverPid=$!
          sleep 5
          kill -9 $serverPid
